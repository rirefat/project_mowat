import React from 'react';
import './MsgModal.css'

const MsgModal = (props) => {
    
    return (
        <div>
            {/* <h1>{props.randomOne.title}</h1> */}
        </div>
    );
};

export default MsgModal;